#!/usr/bin/env bash
set -euo pipefail

# Wrapper that runs the vendor script copied under tests/
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)"
cd "${ROOT_DIR}/tests/ddfun/fortran"

# Execute vendor test script as-is (no extra output)
exec bash ./gnu-ddfun-tests.scr
